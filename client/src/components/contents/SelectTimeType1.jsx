import React from "react";
import TitleType1 from "./TitleType1";
import TableInfoType1b from "./TableInfoType1b";
import { useEffect } from "react";

const TimeTable = ({time,data,btnClick,btnActive,timeClick,timeClick2}) =>{
    const index = 0;
    const index2 = 1;

    /* if(data !== undefined && data !== null && time !== null){
      if(data.reservation_status === `can't` && time ==="10:00"){
         status = 'cant';
      }
      else{
         status  = 'can';
      }
    }
    
    if(btnActive === true && index === 0 ){
      status = 'selected';
   }
    
    if(data !== undefined && data !== null && time !== null){
      if(data.reservation_status === `can't`  && time ==="14:00"){
         status2 = 'cant';
      }
      
      else{
         status2  = 'can';
      }
    }   
    if(btnClick === true && index === 1 ){
      status2 = 'selected';
   } */
    

  
  }
  export default TimeTable;